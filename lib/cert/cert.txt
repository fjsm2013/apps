Perfect 👍 — since you’re on Windows + Apache, here’s the exact, Windows-safe way to extract everything from
froshsystems.com.pfx and wire it into Apache without Linux assumptions.

No guesswork.

🔐 What Apache on Windows needs

Apache cannot use .pfx directly.
You must extract PEM files:

Private key → privkey.pem

Certificate + chain → fullchain.pem

🧰 Prerequisite: OpenSSL on Windows
✅ Check if you already have OpenSSL

Open PowerShell and run:

openssl version


If it works → skip to Step 1.

❌ If not installed

Download Win64 OpenSSL (Light) from:

https://slproweb.com/products/Win32OpenSSL.html

Install it and check “Add OpenSSL to PATH”.

Restart PowerShell after install.

🛠️ Step-by-step: Extract certs on Windows

Assume your file is here:

C:\certs\froshsystems.com.pfx


Create a working folder:

mkdir C:\certs\apache
cd C:\certs\apache

setx OPENSSL_CONF "C:\Program Files\Common Files\SSL\openssl.cnf"

✅ Step 1 — Extract the private key
openssl pkcs12 -in froshsystems.com.pfx -nocerts -nodes -out privkey.pem


Enter the PFX password

This creates:

privkey.pem


⚠️ Keep this file secure.

✅ Step 2 — Extract the certificate
openssl pkcs12 -in froshsystems.com.pfx -clcerts -nokeys -out cert.pem

✅ Step 3 — Extract the CA chain
openssl pkcs12 -in froshsystems.com.pfx -cacerts -nokeys -out chain.pem

✅ Step 4 — Build the full chain (Apache needs this)
type cert.pem chain.pem > fullchain.pem


Now you should have:

privkey.pem
cert.pem
chain.pem
fullchain.pem

🔧 Step 5 — Configure Apache (Windows)

Move the files somewhere clean, e.g.:

C:\Apache24\conf\ssl\froshsystems\


Edit your SSL VirtualHost
(usually C:\Apache24\conf\extra\httpd-ssl.conf or your vhost file):

<VirtualHost *:443>
    ServerName froshsystems.com
    ServerAlias www.froshsystems.com *.froshsystems.com

    SSLEngine on
    SSLCertificateFile "C:/Apache24/conf/ssl/froshsystems/fullchain.pem"
    SSLCertificateKeyFile "C:/Apache24/conf/ssl/froshsystems/privkey.pem"

    DocumentRoot "C:/Apache24/htdocs/froshsystems"
</VirtualHost>


⚠️ Use forward slashes or quoted Windows paths.

✅ Step 6 — Enable SSL modules

In httpd.conf, make sure these lines are uncommented:

LoadModule ssl_module modules/mod_ssl.so
LoadModule socache_shmcb_module modules/mod_socache_shmcb.so
Include conf/extra/httpd-ssl.conf

🔁 Step 7 — Restart Apache

From an Admin Command Prompt:

httpd -k restart


or via Services:

Apache2.4 → Restart

🧪 Step 8 — Test

Open in browser:

https://froshsystems.com

https://www.froshsystems.com

https://test.froshsystems.com

You should see:
🔒 Secure
No warnings
Wildcard working

🔍 Optional verification
openssl s_client -connect froshsystems.com:443 -servername froshsystems.com


Look for:

Verify return code: 0 (ok)

🚨 Common Windows Apache mistakes (avoid these)

❌ Using .pfx directly
❌ Forgetting fullchain.pem
❌ Backslashes without quotes
❌ Apache not restarted
❌ mod_ssl not enabled

🔁 Renewal reminder (important)

Because you used manual DNS-01:

Re-run win-acme every ~60–90 days

Replace .pfx

Re-extract PEMs

Restart Apache

🔥 Future upgrade

Move DNS to Cloudflare → auto-renew → zero TXT pain.

🏁 Final recap

You now have:

✅ Wildcard SSL

✅ Windows-compatible Apache setup

✅ Correct private key + full chain

✅ Production-ready HTTPS

If you want next:

HTTP → HTTPS redirect

SSL hardening (A+ rating)

Apache vhost cleanup

Just tell me — you’ve earned the easy part now